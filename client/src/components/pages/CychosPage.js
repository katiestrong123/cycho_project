import Header from '../Header';
import Footer from '../Footer';

import Cycho from '../Cycho';
import CychoMap from '../CychoMap';
import CychosList from '../CychosList';

const CychosPage = ()  => {

    // should put map here? Can't map of undefined again, I know i woulld have to pass cychos into the function but not sure what was going wrong when i did that 
    return (
        <>

        <CychosList />

        {/* not displaying cychos!!
        
        */}

        
        {/* must have a filter in this page, search for cychos */}
        </>
    )
}

export default CychosPage;